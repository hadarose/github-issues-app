import Container from "@material-ui/core/Container";

const Issues = (props) => {
  // console.log(props);
  // let issuesItems = props.issues.map((issue, index) => {
  //   return <li key={index}>{issue}/></li>;
  // });

  return <h2>{props.name} Issues</h2>;
};

export default Issues;
